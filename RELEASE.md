# Release Process

- Versioning: SemVer
- Packages are versioned together for now.
- Steps:
  - Update versions in `package.json` files.
  - Run `npm run build` and `npm run test`.
  - Tag release `vX.Y.Z` and publish packages.
